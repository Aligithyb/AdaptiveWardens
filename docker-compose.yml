version: '3.8'

services:
  sandbox-store:
    build: ./sandbox-store
    container_name: app-state-store
    volumes:
      - sandbox-data:/data
    networks:
      - prod-internal
      - dmz
    ports:
      - "8001:8001"
    environment:
      - PYTHONUNBUFFERED=1
      - SLACK_WEBHOOK_URL=${SLACK_WEBHOOK_URL}
      - SLACK_BOT_TOKEN=${SLACK_BOT_TOKEN}
      - SLACK_CHANNEL=${SLACK_CHANNEL}
      - CANARY_AWS_ACCESS_KEY=${CANARY_AWS_ACCESS_KEY}
      - CANARY_AWS_SECRET_KEY=${CANARY_AWS_SECRET_KEY}
      - CANARY_STRIPE_KEY=${CANARY_STRIPE_KEY}
      - CANARY_DNS_HOSTNAME=${CANARY_DNS_HOSTNAME}
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "python", "-c", "import requests; requests.get('http://localhost:8001/health')" ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

  ai-engine:
    build: ./ai-engine
    container_name: inference-svc
    networks:
      - prod-internal
      - dmz
    ports:
      - "8002:8002"
    volumes:
      - ./ai-engine/src:/app
    environment:
      - PYTHONUNBUFFERED=1
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - SANDBOX_URL=http://sandbox-store:8001
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    depends_on:
      sandbox-store:
        condition: service_healthy

  ssh-frontend:
    build: ./ssh-frontend
    container_name: auth-gateway
    ports:
      - "2222:2222"
    networks:
      - prod-internal
      - dmz
    volumes:
      - ./ssh-frontend/src/ssh_server.py:/app/ssh_server.py
    environment:
      - SANDBOX_URL=http://sandbox-store:8001
      - AI_ENGINE_URL=http://ai-engine:8002
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "echo 'x' > /dev/tcp/localhost/2222 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 5s
    depends_on:
      sandbox-store:
        condition: service_healthy
      ai-engine:
        condition: service_started

  http-frontend:
    build: ./http-frontend
    container_name: web-gateway
    ports:
      - "8080:8080"
    networks:
      - prod-internal
      - dmz
    volumes:
      - ./http-frontend/src:/app
    environment:
      - SANDBOX_URL=http://sandbox-store:8001
      - AI_ENGINE_URL=http://ai-engine:8002
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "echo 'x' > /dev/tcp/localhost/8080 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 5s
    depends_on:
      sandbox-store:
        condition: service_healthy
      ai-engine:
        condition: service_started

  dashboard-backend:
    build: ./dashboard-backend
    container_name: dashboard-backend-svc
    ports:
      - "8003:8003"
    networks:
      - prod-internal
      - dmz
    volumes:
      - sandbox-data:/data
      - ./dashboard-backend/src:/app
    environment:
      - DB_PATH=/data/app_state.db
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import requests; requests.get('http://localhost:8003/health')"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 5s
    depends_on:
      sandbox-store:
        condition: service_healthy

  dashboard-frontend:
    build: ./dashboard-frontend
    container_name: soc-dashboard
    ports:
      - "3000:3000"
    networks:
      - prod-internal
      - dmz
    volumes:
      - ./dashboard-frontend:/app
      - /app/node_modules # Don't overwrite node_modules with host ones
    environment:
      - NODE_ENV=development
    restart: unless-stopped
    depends_on:
      - dashboard-backend
      - sandbox-store

networks:
  prod-internal:
    driver: bridge
    internal: true
  dmz:
    driver: bridge

volumes:
  sandbox-data:
